const express = require('express')
const router = express.Router()
const { cache, setCache } = require('../services/cache.js')


router.post('/get-products', async (req, res) => {
    try {
       
    }
    catch (err) {
        
    }
})

module.exports = router